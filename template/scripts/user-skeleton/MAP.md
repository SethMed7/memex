# MAP — the index

The spine: every note with a one-line summary so an LLM can decide what to open without reading
everything. Regenerated deterministically by `scripts/organize.ts` (no LLM). Edit summaries in each
note's `summary:` frontmatter — they flow here.

_(Run `bun scripts/organize.ts` to populate this from your notes.)_
