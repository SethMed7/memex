---
summary: one line an LLM reads to decide relevance without opening the file
source: https://example.com   # optional — where this knowledge came from
resource: false               # true if this source is ALSO in clients/resources.json (a live thing an app fetches under guard)
tags: [reference]
updated: YYYY-MM-DD
---

# Name

What this is, in a sentence or two — a fact, how-to, definition, or a record of an external thing worth keeping.

**Why it's useful.** What you'd come back to this note to learn or check.

## Related

[[some-note]]

<!-- A reference is what you KNOW (this note); a resource is where you GO (a source an app fetches from).
     If a connected app should FETCH from this on a cadence — not just keep the note — register it in
     clients/resources.json (set its `reference:` to this note's slug) and set `resource: true` above.
     The note stays the trusted human description; fetched bytes are always treated as untrusted. -->
