# trash/ — soft-deleted (purgeable)

The last stop before deletion. `git mv` a note here to take it out of the active wiki without losing it;
purge whenever (history stays recoverable via git). The validator ignores it.
